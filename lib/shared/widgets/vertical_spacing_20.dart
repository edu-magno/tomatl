import 'package:flutter/material.dart';

class VerticalSpacing20 extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Container(
        margin: EdgeInsets.only(top: 20),
      );
}
