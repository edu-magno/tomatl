import 'package:flutter/material.dart';

class TimerCountWidget extends StatelessWidget {
  final int timer;

  TimerCountWidget({required this.timer});

  @override
  Widget build(BuildContext context) {
    return Text('');
  }
}
